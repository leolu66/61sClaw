# 工作日志 2026-03-03

## 任务

1. **API 余额查询输出格式优化**
   - 将脚本输出改为 JSON 格式
   - 在 SKILL.md 中定义输出模板规范
   - 实践"底层 JSON + 上层模板 + 大模型格式化"的分层设计

2. **对比基准 bug 修复**
   - 问题：多次查询时"与上次对比"没有变化
   - 原因：代码用 `[-1]` 获取当天第一条记录（实际是最旧的），且在保存后才获取基准导致读到自己的记录
   - 解决：
     - 改为 `[0]` 获取最新记录
     - 在保存**之前**获取对比基准，存入结果的 `_baseline` 字段

3. **输出模板分层设计原则**
   - 记录到 MEMORY.md
   - 分层：Python 输出 JSON → SKILL.md 定义模板 → 大模型格式化展示
