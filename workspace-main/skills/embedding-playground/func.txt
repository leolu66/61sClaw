function renderModelCheckboxes(){const activeModels=models.filter(m=>m.is_active);const html=activeModels.map(m=>`<label class="checkbox-item"><input type="checkbox" value="${m.id}" data-name="${escapeHtml(m.name)}" data-dim="${m.dimension}"><span>${escapeHtml(m.name)} (${m.dimension}缁?</span></label>`).join('');['generate','compare','similarity','viz'].forEach(tab=>{const container=document.getElementById(`${tab}-model-checkboxes`);if(container){if(activeModels.length===0){container.innerHTML='<p style="color:var(--text-secondary)">娌℃湁鍙敤鐨勬ā鍨嬶紝璇峰厛鍦?妯″瀷绠＄悊"涓惎鐢ㄦā鍨?/p>'}else{container.innerHTML=html;if(tab==='similarity'){container.querySelectorAll('input[type="checkbox"]').forEach(cb=>{cb.addEventListener('change',(e)=>{if(e.target.checked){container.querySelectorAll('input[type="checkbox"]').forEach(other=>{if(other!==e.target)other.checked=false}})}})}}}})}
